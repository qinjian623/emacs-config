;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject" "c:/Documents and Settings/qinjian/Application Data/.emacs.d/elpa/eproject-0.4")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 110)
        (left . 110)
        (width . 80)
        (height . 38)))

